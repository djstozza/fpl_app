<%= form_for(fpl_team_list) do |f| %>
  <% if fpl_team_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fpl_team_list.errors.count, "error") %> prohibited this fpl_team_list from being saved:</h2>

      <ul>
      <% fpl_team_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
