<%= form_for(fpl_team) do |f| %>
  <% if fpl_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fpl_team.errors.count, "error") %> prohibited this fpl_team from being saved:</h2>

      <ul>
      <% fpl_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
